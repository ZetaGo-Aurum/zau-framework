run = "zau dev --port 8000"
entrypoint = "backend/app.py"

[nix]
channel = "stable-24_05"

[deployment]
run = ["zau", "start", "--host", "0.0.0.0", "--port", "8000"]
deploymentTarget = "cloudrun"

[[ports]]
localPort = 8000
externalPort = 80
